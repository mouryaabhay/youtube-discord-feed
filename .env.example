# ---------- Repository Secrets ---------- #

# Required for GitHub Actions webhook mode. This is the only value that needs
# to be secret; every other configurable value lives in src/configs/config.js.
DISCORD_WEBHOOK_URL=
