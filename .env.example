# ---------- Repository Secrets ---------- #

# Required for GitHub Actions webhook mode. Keep credentials in GitHub Actions
# Secrets or a local .env file; other configuration lives in src/configs/config.js.
DISCORD_WEBHOOK_URL=
# Optional Discord role ID to ping in the message content outside the embed.
DISCORD_ROLE_ID=
